// This codes tries to flatten a list with three pointers, namely, next, prev and child.
// It is like a tree, so the tree traversal approach can work.
// But the algorithm here is not recursive and it uses less memory.
// The complexity is O(2n), or O(n).

void flattenList(Node *head, Node **tail){
	
}